# Single line comment
let version = 1 + (50 / 2) + (8 * 3);
puts(version);

let name = "The Monkey programming language";
puts(name);

let isMonkeyFastNow = true;
if (isMonkeyFastNow) {
    puts("Yes, Monkey is fast now!");
}

let people = [{"name": "Anna", "age": 24}, {"name": "Bob", "age": 99}];
puts(people);